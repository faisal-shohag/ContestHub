import { Outlet } from "react-router-dom";

const CreatorDashboard = () => {
    return (
        <div>
            Creator Dashboard
            <Outlet/>
            
        </div>
    );
};

export default CreatorDashboard;